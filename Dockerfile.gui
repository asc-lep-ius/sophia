# Multi-stage build for the Sophia GUI service (CPU-only).
#
# Stages:
#   base        — slim Python image with uv installed
#   cpu-builder — installs all deps with CPU-pinned torch
#   runtime     — minimal image copying the pre-built venv
#
# For GPU support see Dockerfile.gui.cuda.

# ── Base ───────────────────────────────────────────────────────────────────────
FROM python:3.12-slim AS base
ENV TERM=xterm-256color LANG=C.UTF-8 LC_ALL=C.UTF-8
RUN pip install uv

# ── CPU builder ────────────────────────────────────────────────────────────────
# Installs all deps with CPU-pinned torch; venv copied into runtime stage.
FROM base AS cpu-builder
WORKDIR /app
COPY pyproject.toml uv.lock README.md ./
COPY src/ src/
RUN uv sync --extra web --no-dev --frozen && \
    . .venv/bin/activate && \
    uv pip install torch --index-url https://download.pytorch.org/whl/cpu && \
    uv pip install faster-whisper chromadb sentence-transformers openai

# ── Runtime ────────────────────────────────────────────────────────────────────
FROM base AS runtime
WORKDIR /app
ENV SOPHIA_DATA_DIR=/data
RUN apt-get update && apt-get install -y --no-install-recommends ffmpeg && rm -rf /var/lib/apt/lists/*
COPY --from=cpu-builder /app/.venv /app/.venv
COPY --from=cpu-builder /app/src /app/src
COPY --from=cpu-builder /app/pyproject.toml /app/pyproject.toml
COPY --from=cpu-builder /app/README.md /app/README.md
RUN useradd --create-home --uid 1000 sophia
RUN mkdir -p /data /app/.nicegui /home/sophia/.cache/huggingface \
    && chown sophia:sophia /data /app/.nicegui /home/sophia/.cache/huggingface
EXPOSE 8080
HEALTHCHECK --interval=30s --timeout=5s --retries=3 --start-period=10s \
    CMD ["python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health')"]
ENV HF_HOME=/home/sophia/.cache/huggingface
USER sophia
CMD ["/app/.venv/bin/python", "-m", "sophia", "gui", "--host", "0.0.0.0"]
